# Task OS-010A: Resumable Historical Catalog Full Audio QC

## Summary

Run deterministic, resumable, read-only full audio QC across the human-approved
historical catalog WAV map. Record PCM integrity, source hashes, EBU integrated
loudness, loudness range, true peak, clipping, and radio-derivative need without
modifying masters or creating derivatives.

## Metadata

- **Ticket ID**: `OS-010A`
- **Status**: `COMPLETE`
- **Assignee**: `Codex`
- **Component**: `catalog / ingestion staging / audio QC`
- **Parent Ticket**: `OS-010`

## Scope

- [x] Strict explicit `source_row,wav_path` input; no filename guessing.
- [x] Atomic checkpoint after every track and safe resume of completed rows.
- [x] FFmpeg/FFprobe version provenance in the report.
- [x] Continue independent tracks after a per-file QC error.
- [x] Execute full QC for the 136 playlist-eligible mappings.
- [x] Produce a human review summary for derivative planning.

## Safety Boundary

- Masters are read-only and verified unchanged by `inspect_wav`.
- No normalization, transcoding, movement, renaming, deletion, or derivative output.
- No AST identifier allocation or canonical repository writes.
- No rights, ownership, clearance, rotation, or metadata promotion.
- No changes to radio runtime, overlays, Liquidsoap, systemd, publisher, VPS,
  YouTube, stream keys, schemas, VISUAL-003, OS-009, or RADIO-006.

## Outputs

- Runner: `scripts/run_catalog_audio_qc.py`
- Tests: `tests/test_catalog_audio_qc_runner.py`
- Generated reports: ignored local state only.

## Completion Evidence

- Full QC completed for `136/136` playlist-eligible WAV mappings with `0` file
  failures.
- `135` tracks require a separate radio derivative under the provisional
  `-15..-13 LUFS` and `<= -1 dBTP` planning thresholds.
- `Breathe Out of Funk` is the one source already within those provisional
  thresholds (`-14.69 LUFS`, `-1.18 dBTP`).
- `17` source masters contain one or more PCM samples at the digital limit and
  are explicitly prioritized for clipping review before derivative generation.
- Source format inventory: `99` at 44.1 kHz / 16-bit stereo, `36` at 48 kHz /
  16-bit stereo, and `1` at 48 kHz / 24-bit stereo.
- No master was normalized, transcoded, moved, renamed, or otherwise modified.
- Focused verification after repository reconciliation: `12` OS-010/OS-010A
  audio tests pass.
- Repository-wide verification ran `241` tests. It is not clean because the
  current default Python environment lacks `jsonschema` (`53` errors) and two
  unrelated review-runner expectations fail; these failures are outside OS-010A.

## Acceptance Criteria

- Same WAV map cannot resume from a checkpoint for different input bytes.
- A completed row is not re-analyzed on resume; its source bytes are re-hashed
  before accepting the checkpoint.
- Report distinguishes master validity from radio-derivative need.
- Failures remain explicit and never become guessed passes.
- Source WAV bytes are unchanged.
