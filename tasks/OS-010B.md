# Task OS-010B: Six Daypart Radio Derivative Proof

## Summary

Create and verify separate local radio WAV derivatives for the six frozen historical
catalog daypart representatives. Preserve every source master byte-for-byte and keep
all generated audio outside canonical catalog and live radio paths.

## Metadata

- **Ticket ID**: `OS-010B`
- **Status**: `COMPLETE`
- **Assignee**: `Codex`
- **Component**: `catalog / offline audio derivatives`
- **Parent Ticket**: `OS-010A`

## Approved Scope

- [x] Human authorization received through `go OS-010B`.
- [x] Use exactly one already-frozen representative for each of six dayparts.
- [x] Use two-pass FFmpeg EBU R128 loudness normalization.
- [x] Write atomic stereo PCM WAV derivatives at 44.1 kHz / 16-bit.
- [x] Target approximately `-14 LUFS` with a processing ceiling of `-1.5 dBTP`.
- [x] Verify final loudness, `<= -1 dBTP`, duration, PCM format, clipping, and hashes.
- [x] Execute the six-track local proof and record evidence.

## Safety Boundary

- Source WAVs are read-only and must retain their approved OS-010A SHA-256 values.
- Derivatives and reports are local staging outputs, not canonical assets.
- Existing output files fail closed; the generator never silently overwrites them.
- Source clipping is recorded as lineage evidence; normalization does not claim to
  repair clipping already present in a master.
- No rights approval, AST allocation, catalog import, playlist publication, or live
  radio integration.
- No changes to overlays, VISUAL-003, RADIO-006, OS-009, Liquidsoap, systemd,
  production publisher, VPS, YouTube, stream keys, or schemas.

## Files

- Generator: `scripts/create_radio_derivatives.py`
- Tests: `tests/test_radio_derivatives.py`
- Generated WAVs and JSON report: conversation-local ignored output only.

## Completion Evidence

- All six frozen daypart representatives completed with `0` failures.
- Every derivative is stereo 44.1 kHz / 16-bit PCM WAV with zero clipped PCM
  samples and independently verified true peak at or below `-1 dBTP`.
- Verified integrated loudness values: SUNRISE `-14.31`, DAY `-14.00`, FUTURE
  `-13.99`, SUNSET `-14.00`, NIGHT `-14.00`, DEEP SPACE `-14.00` LUFS.
- Every source SHA-256 matched the approved OS-010A input before processing and
  again after output verification.
- The two clipped sources remain flagged in lineage: SUNRISE has `91` stored
  clipped PCM samples and DAY has `474`. Their new derivatives contain zero;
  this does not claim to repair historical source clipping.
- Focused verification after repository reconciliation: `22`
  OS-010/OS-010A/OS-010B audio tests pass.
