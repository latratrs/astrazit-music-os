# Task OS-010C: Historical Catalog Radio Derivative Batch

## Summary

Use the verified OS-010B offline profile to create separate local radio WAV
derivatives for the remaining 130 playlist-eligible historical catalog masters.

## Metadata

- **Ticket ID**: `OS-010C`
- **Status**: `COMPLETE`
- **Assignee**: `Codex`
- **Component**: `catalog / offline audio derivatives`
- **Parent Ticket**: `OS-010B`

## Scope

- [x] Human authorization received through `go OS-010C`.
- [x] Derive the exact input map from OS-010A source-row/hash/duration evidence.
- [x] Exclude the six already-completed OS-010B representative rows.
- [x] Use resumable, bounded-concurrency two-pass FFmpeg normalization.
- [x] Preserve source hashes and fail closed on source/output mismatch.
- [x] Complete the 130-track local derivative batch and final independent audit.

## Safety Boundary

- Outputs are local staging WAVs only; no source master is replaced or modified.
- Existing output files are only resumed when their recorded SHA-256 matches.
- Resumed rows also require the current source WAV SHA-256 to match the approved
  OS-010A source hash.
- No rights, clearance, AST IDs, canonical catalog writes, playlist activation, or
  live radio integration.
- No changes to apps/radio runtime, VISUAL-003, RADIO-006, OS-009, Liquidsoap,
  systemd, publisher, VPS, YouTube, stream keys, or schemas.

## Files

- Batch runner: `scripts/run_catalog_radio_derivatives.py`
- Tests: `tests/test_catalog_radio_derivatives.py`
- Generated WAVs and report: conversation-local ignored output only.

## Completion Evidence

- All `130/130` remaining playlist-eligible source rows have a verified local
  derivative. The final output set contains `130` unique WAV files.
- Standard profile completed `127` tracks. Three explicit dynamic-loudness
  exceptions were retried separately using the same `-14 LUFS` target, the
  calibrated `-14.8..-13.5 LUFS` verification band, and a stricter `-1.6 dBTP`
  processing ceiling.
- Retry results: Unconditional Love (`-14.01 LUFS`, `-1.09 dBTP`), Smooth Sally
  (`-14.75 LUFS`, `-1.43 dBTP`), and Magic Country (`-14.59 LUFS`, `-1.34 dBTP`).
- All `130` outputs are stereo 44.1 kHz / 16-bit PCM WAV, with zero clipped PCM
  samples and true peak no higher than `-1 dBTP`.
- All `130` source hashes matched OS-010A evidence before and after derivative
  creation. No partial WAV files remain.
- Focused verification after repository reconciliation: `29`
  OS-010/OS-010A/OS-010B/OS-010C audio tests pass.
